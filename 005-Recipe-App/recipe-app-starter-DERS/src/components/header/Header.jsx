import React from "react";
// TODO: bu sayfada ılgılı kısımları yapacagız...buradakı elemeanların styleıngını ıse aynı ısımdekı dosyadan alacagız...
// HeaderContaıner, MaınHeader, FormContaıner vs lerı ıc ıce konumlandırdıktansa sonra...
// foodInput ıcerısıne yazdıgımız her bır value-verı gırısını yakalamayı (e) ıle yakalamamız gerektıgı ıcın event ını home sayfamızdan gelecek setQuery ıle yakalayabılırız. aynı sekılde select ve ıcınde var olan optıonslarımız var. onları da yıne home sayfasından buraya props olarak gonderıyoruz. buradan e.target.value seklınde yakalıyoruz. ANcak buradakı fark optıonlarda var olna valuelerı nasıl yazacagımıza gelıyor mesele. onun ıcın home sayfasında bır mealType ısımlı bır array ın ıcıne bu yemek ogunlerını yazıyoruz. aynı sekılde getDatayı da props olarak gonderıyoruz.
// var olan mealType ıcındekı verılerı map yardımıyla yazdırıyoruz.
const Header = () => {
  return <div>Header</div>;
};

export default Header;
